/**
 * Pra Theme main script. Loaded from the footer with defer (see functions.php).
 * Vanilla JS only — the theme ships zero jQuery.
 */
document.addEventListener('DOMContentLoaded', () => {
	// Your code here.
});
